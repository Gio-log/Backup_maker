<Window x:Class="Backup_Maker.Views.Windows.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Backup_Maker.Views.Windows"
        xmlns:userControls="clr-namespace:Backup_Maker.Views.UserControls"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="PopupWindow" Height="200" Width="400" FontSize="15" MinWidth="200" MinHeight="200">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="0" />
    </WindowChrome.WindowChrome>
    <Grid Background="#252525">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <userControls:TitleBar Title="{Binding WindowTitle}"
        Grid.Row="0" Grid.ColumnSpan="2"/>
        <TextBlock Text="{Binding Message}" Foreground="White"  
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Grid.Row="1" Grid.ColumnSpan="2"/>
        <Rectangle Grid.Row="2" />
        <Button x:Name="Yes" Content="Yes" Grid.Row="2" Grid.Column="0" Visibility="Collapsed" 
                HorizontalAlignment="Stretch" Margin="10,0,10,5" VerticalAlignment="Center"
                Click="Yes_Click" />
        <Button x:Name="No" Content="No" Grid.Row="2" Grid.Column="1" Visibility="Collapsed" 
                HorizontalAlignment="Stretch" Margin="10,0,10,5" VerticalAlignment="Center"
                Click="No_Click" />
        <Button x:Name="Ok" Content="Ok" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Visibility="Collapsed" 
                HorizontalAlignment="Stretch" Margin="10,0,10,5" VerticalAlignment="Center"
                Click="Ok_Click" />
    </Grid>
</Window>
