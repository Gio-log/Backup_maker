<Window x:Class="Backup_Maker.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Backup_Maker"
        xmlns:userControls="clr-namespace:Backup_Maker.Views.UserControls"
        mc:Ignorable="d"
        WindowStyle="None"
        Icon="/Resources/logo.ico"
        Title="Backup Maker" Height="500" Width="950" FontSize="15" MinWidth="690" MinHeight="400">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="0" />
    </WindowChrome.WindowChrome>
    <Grid Background="#282828">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="1*" MinWidth="320" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="1*" MinWidth="320" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <userControls:TitleBar Title="Backup Maker"
            Grid.Row="0" Grid.ColumnSpan="5"/>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding FilesLocation}" Name="filesLocationTextBlock" Foreground="White"
                       Grid.Row="0" VerticalAlignment="Center" />
            <CheckBox Content="Overwrite"
                      Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" VerticalContentAlignment="Bottom"
                      Grid.Row="1" IsChecked="{Binding Overwrite}"/>
        </Grid>
        <userControls:BackupControls 
            Grid.Row="1" Grid.Column="3"/>
        <GridSplitter Background="#252525" Grid.Column="2" Grid.Row="0" Grid.RowSpan="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <userControls:SearchBar SearchFile="{Binding SearchFile, Mode=TwoWay}" Placeholder="Input text to search a file"
            Grid.Row="3" Grid.Column="1"/>
        <userControls:SearchBar SearchFile="{Binding SearchBackup, Mode=TwoWay}" Placeholder="Input text to search a backup"
            Grid.Row="3" Grid.Column="3"/>
        <userControls:FileList 
            Label="Files"
            ItemsSource="{Binding FileDatas}"
            SelectedItem="{Binding SelectedFileData, Mode=TwoWay}"
            SelectedItems="{Binding SelectedFiles, Mode=OneWay}"
            Grid.Row="2" Grid.Column="1"/>
        <userControls:FileList 
            Label="Backups"
            ItemsSource="{Binding Backups}"
            SelectedItem="{Binding SelectedBackup, Mode=TwoWay}"
            SelectedItems="{Binding SelectedBackups, Mode=OneWay}"
            Grid.Row="2" Grid.Column="3"/>
    </Grid>
</Window>
